import { DateToStrPipe } from './date-to-str.pipe';

describe('DateToStrPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToStrPipe();
    expect(pipe).toBeTruthy();
  });
});
