import { FieldErrorValidationDirective } from './field-error-validation.directive';

describe('FieldErrorValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldErrorValidationDirective();
    expect(directive).toBeTruthy();
  });
});
